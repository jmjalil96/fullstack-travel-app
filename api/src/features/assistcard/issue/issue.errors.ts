/**
 * Issue endpoint uses the same error handling as other Assistcard endpoints
 * Re-exporting from products for consistency
 */
export { AssistcardApiError, createAssistcardApiError } from '../products/products.errors.js'
